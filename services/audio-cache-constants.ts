/** Nama cache audio — selaras `public/sw.js` (`CACHE_AUDIO`). */
export const AUDIO_CACHE_NAME = 'hanquran-audio-v1';

/** Versi manifest unduhan per surat. */
export const DOWNLOAD_MANIFEST_VERSION = 'v1';

/** Unduhan paralel maksimum per surat (docs/15 §12.4). */
export const DOWNLOAD_CONCURRENCY = 3;
